/**
 * 
 */
/* 	
	
	$(function() {
	    var dialog, form;
	  
	 
	    dialog = $( "#dialog-form" ).dialog({
	      autoOpen: false,
	      height: 500,
	      width: 550,
	      modal: true,
	      buttons: {
	        Registra: function(){$( "#regDoc\\:agregarDr" ).click()},
	        Cancelar: function() {
	          dialog.dialog( "close" );
	        }
	      },
	      close: function() {
	        form[ 0 ].reset();
	        
	      }
	    });
	 
	    //form = dialog.find( "form" ).on( "submit", function( event ) {
	      //event.preventDefault();
	      //addUser();
	    //});
	 
	    $( "#registro" ).button().on( "click", function() {
	      dialog.dialog( "open" );
	    });
	    
	    
	  });
*/